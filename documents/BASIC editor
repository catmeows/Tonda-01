;LINE EDITOR:
;
;lineEditorInit
;    currentLine=0
;lineEditorLoop
;    lastListedLine = list(currentLine, noScroll)
;    key = readKey
;    if key==UP
;        if (currentLine>1)
;            currentLine=currentLine-1
;    if key==DOWN
;        if (lineExists(currentLine+1))
;            currentLine=currentLine+1
;    if key==RIGHT
;        if (currentLine>1)
;            editLine(currentLine)
;    if key==CTRL&DOWN
;        currentLine = lastListedLine
;    if key==CTRL&UP
;        if (currentLine>5)
;            currentLine = currentLine - 5
;    if

